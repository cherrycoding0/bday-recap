# 구현 노트 (2026-08-20, Claude 작업)

지시에 없어서 스스로 판단한 것들의 기록. 마음에 안 들면 여기 적힌 위치만 고치면 됨.

## 확신도 '중' — 먼저 확인해주세요

### 팔레트를 성호 최애 색(라이트 퍼플) 기반으로 확정
- 상황: "앨범/컨셉 사진에서 추출한 컬러로 교체" TODO가 있었으나 어떤 앨범 기준인지 미지정
- 선택: 성호의 최애 색 '라이트 퍼플'(Melon TMI 인터뷰) 기반 라벤더 팔레트
- 대안: 앨범 아트 추출색 — 앨범마다 달라 기준이 애매했음
- 되돌리기: `config/artist.ts`의 `themeColor` 값만 교체
- 참고: `card`(흰색) 색을 추가함 — 기존엔 페이지 배경과 카운트다운 타일이 같은 색이라 타일이 안 보였음

### 제목 폰트 = 배민 주아체(BMJUA)
- 상황: "눈누에서 고른 폰트" TODO — 어떤 폰트인지 미지정
- 선택: 본문 Pretendard(OFL) + 제목 배민 주아체(우아한형제들 무료 배포, 웹 임베딩 허용). 둘 다 npm으로 받아 셀프호스팅 → 빌드 시 네트워크 불필요
- 대안: Cafe24 Ssurround — npm에 없어 CDN 의존이 생겨 제외
- 되돌리기: `app/fonts/`에 woff2 넣고 `app/layout.tsx`의 localFont 경로만 교체

### AI 리캡 = 서버 라우트 `/api/recap` + 텍스트 카드
- 상황: "AI 리캡 카드 생성 로직"의 형태 미지정
- 선택: GET /api/recap → Supabase에서 메시지 로드 → Anthropic/OpenAI 중 있는 키로 요약(JSON) → 카드 렌더. 키가 없으면 통계 기반 기본 리캡으로 폴백(항상 동작)
- 대안: 이미지 파일 생성(html2canvas 등) — 의존성이 늘어 v1에선 제외, "텍스트 복사" 버튼으로 대체
- 되돌리기: `app/api/recap/route.ts`, `components/RecapCard.tsx`
- ⚠️ [미검증] 실제 AI 키로 호출하는 경로는 키가 없어 실행 못 해봄. 코드상 실패 시 기본 리캡 폴백. 모델명은 RECAP_MODEL 환경변수로 교체 가능(기본 claude-haiku-4-5 / gpt-4o-mini)

## 확신도 '상'

### 필터 강화 (`lib/filter.ts`)
링크(dot/점 우회 포함), 연락처/오픈채팅 유도, 같은 글자 8회 이상 반복, 비속어(공백·특수문자 우회 정규화 후 검사). 닉네임에도 동일 적용. Playwright로 3종 차단 + 정상 메시지 통과 확인함.
차단어 목록은 파일 상단 `BLOCKED_WORDS` — 팬덤 상황에 맞게 자유롭게 추가.

### 카운트다운 hydration 수정
서버 렌더와 클라이언트 시간 차로 hydration mismatch 경고 가능성 → 마운트 전 "--" 표시로 수정.

### 다크모드 CSS 제거
기존 globals.css의 `prefers-color-scheme: dark` 오버라이드가 라벤더 배경 위 글자를 안 보이게 만들 수 있어 제거. 페이지는 아티스트 팔레트로만 칠함.

### OG 이미지
`public/og-image.png` (1200x630, 49KB). 저작권 문제가 없도록 사진 없이 팔레트+타이포로만 제작. `scripts/`에 재생성 스크립트는 안 남김(1회성) — 바꾸고 싶으면 아무 이미지나 같은 파일명으로 교체.

## 검증 결과 (이번 세션에서 실제 실행)
- `next build` 통과 (TypeScript 검사 포함)
- Playwright: 모바일(480px)·데스크톱(1280px) 렌더 확인, 필터 3종 차단 확인, 미리보기 모드 메시지 등록 확인, Supabase 미연결 시 리캡 에러 문구 확인
- [미검증] Supabase 실연결 상태의 저장/리캡, Vercel 배포


---

# v1.5 구현 노트 (2026-08-20, 같은 세션)

## 확신도 '상'

### 테스트 → 카드 플로우
- 콘텐츠는 전부 `config/quiz.ts` 한 파일 (✏️ 주리 수정 지점). 파트/직캠이 비면 "준비 중"으로 표시되고 직캠 버튼은 숨김 — placeholder 상태로도 완전 동작.
- 점수 매핑: 2축(tension/social) 합산 → `resolveType()`. 균형(둘 다 ±1 이내)이면 프로형. 결과 분포가 이상하면 이 함수만 조정.
- 카드 이미지는 클라이언트 캔버스로 생성(1080x1350, 4:5) — 외부 이미지 없이 팔레트+타이포만 (저작권·CORS 안전). 모바일은 이미지 꾹 눌러 저장.

### DB v1.5 (`sql/upgrade-v15.sql`, 실행 완료)
- `seq` identity 컬럼(순번), `hearts` 컬럼, `heart_message(uuid)` RPC(security definer — 익명이 hearts만 +1 가능, content 수정 불가).
- 검증: REST insert 201 + seq 반환, RPC 200 + hearts=1, 테스트 행 삭제 완료.

### 비용 방어
- `/api/comment`: IP당 분당 3회 제한, 초과·무키·실패 시 유형별 템플릿 폴백 → 카드 발급은 절대 안 막힘.
- `/api/recap`: 1시간 인메모리 캐시(+메시지 20개 이상 변동 시 무효화), IP당 분당 5회. 서버리스 인스턴스별 캐시라 완벽하진 않지만 어뷰징 완화 목적으론 충분.

## 확신도 '중' — 확인해주세요

### 하트 중복 방지는 localStorage 기준
- 시크릿모드/기기 변경으로 우회 가능. 팬 이벤트 성격상 과한 방어는 불필요하다고 판단. 문제되면 서버측 IP 기반으로 강화.

### 페이지가 클라이언트 컴포넌트로 전환됨
- 904 카운터/목록 갱신 연동(refreshKey) 때문에 `app/page.tsx`를 "use client"로. 메타데이터는 layout.tsx에 있어 SEO 영향 없음.

## 검증 결과 (실제 실행)
- `next build` 통과 (env 있는 상태/없는 상태 각 1회)
- Playwright(모바일 420px): 테스트 5문항 → 결과(승부사형) → 필터 차단 → 카드 생성(#0001, AI 폴백 코멘트 포함) 전체 플로우 통과
- Supabase 실연결: insert/seq/하트 RPC 검증 (주리 브라우저 경유)
- [미검증] 실제 AI 키로의 comment/recap 호출, Vercel 배포, D-day 모드의 실제 자정 전환(시뮬레이션 안 함 — `isBirthdayNow()`가 단순 시간비교라 위험 낮음)

### 유형당 파트 랜덤 (2026-08-20 추가)
- `FanType.parts`가 배열로 변경 — 유형당 곡 2~3개를 넣으면 결과마다 랜덤 선택.
- 파트가 2개 이상이면 "다른 파트도 보기 🔀" 버튼 표시 — 리롤 시 직전 파트는 제외(항상 바뀜).
- 재도전/리롤이 체류·재방문 포인트가 되는 부수 효과. E2E로 랜덤·리롤·썸네일 연동 검증 완료.
